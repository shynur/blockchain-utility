## 任务

实现网页版 BIP 44 钱包.

## 技术路线

### 技术栈

纯前端 web 应用, 可离线使用.

仅使用:
- JavaScript (JSDoc)
- CSS
- HTML
不依赖任何第三方库.

### 底层库

底层库提供的 API 都有 JSDoc.

#### BIP 32

API 由 `../bip32/index.mjs` 提供.

使用示例可以参考
- `../bip32/main.mjs`
- `../bip32/index.mjs`

#### BIP 39

API 由 `../bip39/index.mjs` 提供.

使用示例可以参考
- `../bip39/main.mjs`
- `../bip39/index.mjs`

## UI & UX

### word/passphrase 输入框

App 根据输入的单词数量, 自动判断使用了哪种词数的方案.
有了助记词和 passphrase, app 内部可以生成 master private key 了.

一旦输入合法, 立即当成最终输入.
每次更改输入, 即检测输入是否合法, 并重新生成 master private key.

#### 助记词 (仅支持英文)

##### 合法字符

- 英文字母
- 空白字符

##### 输入过滤

- 忽略所有 非法字符 的 击键/文本粘贴.

- 输入的文本必须以英文字母开头, 否则
  - 忽略相应的击键
  - 如果是粘贴的文本, 则一直 trim 到首个非空白字符

##### 隐私保护

正在输入单词中, 单词完整显示.
删除仅删除一个字符.

当输入空白字符时, 表示刚才那个单词已输入完毕, 显示成 `①②③④⑤⑥⑦⑧⑨⑩⑪⑫⑬⑭⑮⑯⑰⑱⑲⑳㉑㉒㉓㉔` 中的一个, 表示第 N 个 word.
同时, 开始输入下一个单词.
单词之间多余的空白字符会被忽略, 仅保留一个空格.

当下一个单词还没字母时, 所有单词都是 `①②③④⑤⑥⑦⑧⑨⑩⑪⑫⑬⑭⑮⑯⑰⑱⑲⑳㉑㉒㉓㉔` 中的某一个, 此时删除一次, 会删除前面的整个单词.

(用户可以在输入全部的助记词之后, 故意追加一个空格, 使得最后一个 word 也隐藏起来.)

#### passphrase

位于 word 输入框之后.

##### 合法字符

- 非空白字符

##### 输入过滤

- 忽略所有 非法字符 的 击键/文本粘贴.

##### 隐私保护

每个字符都会显示成 `*`.

### xpub/xprv 输入框

入口除了 word/passphrase 输入框, 还有 xpub/xprv 输入框.

word 输入框检测到粘贴或击键的第一个非空白字符是 x 后,
则对后面的字符继续检测, 如果是完整的 `xpub` 或 `xprv`,
则 word 输入框会将输入当成 xpub/xprv 处理, 此时切换过滤规则, 只允许 base58 字符被写入;
同时 passphrase 输入框被隐藏, 但已输入的 passphrase 会保留.

输入 xpub 或 xprv 时, 在第 `len(xpub)` (0-indexed) 处显示 `|` 表示合法的文本长度,
供用户核对.

#### xprv

输入 xprv 时, 除了 `xprv` 这个前缀, 后续字符全部显示成 `*`.

### Path 选择

对于导入的 xprv/xpub, 它的 path 是定死的, 只能选择后续的 子 path.

#### `purpose'`

无法选择, 固定 `44': BIP 44`.

#### `coin_type'`

默认 BTC.

格式 `数字': 币种`.

#### `account'`

是一个输入框, 后面有个 `H`.

输入框长度为 1, 会根据输入动态变长.

#### `change`

是一个 switch, 备注会根据 switch 状态切换.

备注格式: `数字: 注释`.

#### `address_index`

输入框, 按回车确认输入.
可以输入多个.

每个输入会加入到已输入列表, 每个元素后面有个 `x` 可以移除该元素.

### 生成 xprv/xpub/K

每个 path 选项后面, 都可以选择是否要在此节点处生成 xprv/xpub/K.

可多选.

#### K

K 表示公钥, 默认勾选.

#### xprv

若无法生成 xprv (例如 使用了 xpub 进行导入), 则压根不显示该选项.

默认使用 `xprv***` (此处省略若干个 `*`) 格式隐藏, 后有一个图标用来切换是否要隐藏.
